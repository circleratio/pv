-*- coding: utf-8 -*-

# TODO

- アプリアイコン（macOS/Linux向け）: 実行ファイルアイコンはWindows向け（`src-tauri/icons/icon.ico`ほか）のみ更新済み。macOS（`icon.icns`）・Linux向けの意匠確認・調整は保留中（ユーザー指示により今回スコープ外）。

## 過去の経緯
- 2026-08-10: requirement.md レビューで判明した10件の未決事項（ウィンドウリサイズ時の面積基準、スライド一覧表示中の新規PDFオープン、レーザーポインターとスライド一覧の相互作用、右クリックメニュー表示中のEsc、ファイル履歴の同一性判定、起動引数の相対パス正規化、履歴再オープン失敗時の扱い、非PDFファイルの扱い、ページごとのアスペクト比差異、ドラッグ＆ドロップの複数ファイル順序）は、いずれも方針が決まり requirement.md / spec.md に反映済み。
- 2026-08-10: 上記に続く再レビューで判明した5件の未決事項は、いずれも方針が決まり requirement.md / spec.md に反映済み。
  - 全画面モード中に開いたPDFのアスペクト比リサイズを、全画面解除時に後追い適用するか → 適用しない（全画面に入る前の通常サイズに戻るのみ）
  - リサイズ後のウィンドウサイズが画面（作業領域）からはみ出す場合の扱い → アスペクト比を保ったまま画面に収まる最大サイズにクランプする
  - レーザーポインターのフェードアウト時間（「1〜2秒程度」の範囲値） → 約1秒に確定
  - スライド一覧表示中の新規PDFオープン時のリサイズと、全画面時リサイズ抑制ルールの優先関係 → 全画面抑制ルールを優先する
  - ダイアログ選択・ドラッグ＆ドロップで取得したパスの絶対パス化方針 → OS/Tauri側が絶対パスを返す前提とし、アプリ側での追加変換は行わない（起動時引数のみアプリ側で変換）
